const { runallPaths } = require("./paths");

const paths = {
  ffmpegPath : "/opt/homebrew/bin/ffmpeg",
  soxPath : "/opt/homebrew/bin/sox",
  runallPath : "http://127.0.0.1:8000/runall"
}

module.exports = paths;

